< in file /repos/canve/test-projects/allenAI-pipeline/src/main/scala/org/allenai/pipeline/SaveToArtifact.scala >

  def create: A = {
    if (!artifact.exists) {
      writer.write(input.get, artifact)
    }
    artifact
  }
